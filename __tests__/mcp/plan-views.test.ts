import { describe, it } from 'vitest';

describe('[001] US9 — Semana con compuerta y alertas en la agenda', () => {
  it.todo('US9-AS1 · la tercera apertura de la vista de semana en la misma semana pide una razón, que queda registrada y contada');
  it.todo('US9-AS2 · la vista de semana muestra cada disparador con su resultado y las tandas por día, sin gráficas');
  it.todo('US9-AS3 · la agenda muestra las alertas de materias abandonadas o sin evaluaciones (ciegas)');
});
