FROM node:22-alpine

WORKDIR /app

# Install dependencies (cached by Docker if package.json does not change)
COPY package*.json ./
RUN npm ci

# Copy source code
COPY . .

ENV NODE_ENV=production
ENV MCP_PORT=3001

EXPOSE 3001

CMD ["npx", "tsx", "mcp-server/index.ts"]
